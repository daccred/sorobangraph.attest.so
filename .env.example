# .env.testnet - Stellar Testnet Configuration
# =============================================
# Use this file for connecting to Stellar's test network
# Copy to .env to activate: cp .env.example .env
# ---
# .env.example
# Database Configuration
DATABASE_URL=postgres://stellar:stellar123@localhost:5432/sorobangraph?sslmode=disable

# Network Configuration (Choose one)
# Testnet
NETWORK_PASSPHRASE=Test SDF Network ; September 2015
HISTORY_ARCHIVE_URLS=https://history.stellar.org/prd/core-testnet/core_testnet_001

# Mainnet (uncomment to use)
# NETWORK_PASSPHRASE=Public Global Stellar Network ; September 2015
# HISTORY_ARCHIVE_URLS=https://history.stellar.org/prd/core-live/core_live_001

# Captive Core Configuration (Optional - for better performance)
# CAPTIVE_CORE_BINARY_PATH=/usr/bin/stellar-core
# CAPTIVE_CORE_CONFIG_PATH=/etc/stellar-core.cfg

# Ingestion Configuration
# -----------------------
# Start from genesis (0) or specific ledger number
START_LEDGER=880500
# End at specific ledger (0 = continuous streaming)
END_LEDGER=0

# API Configuration
# -----------------
# Enable WebSocket support for real-time updates
ENABLE_WEBSOCKET=true
# API server port (default: 8080)
PORT=8080

# Logging Configuration
# ---------------------
# Log level: debug, info, warn, error
GIN_MODE=debug
LOG_LEVEL=info

# Captive Core Configuration (Optional)
# -------------------------------------
# For real-time ingestion without relying on history archives
# Uncomment and configure the following if you have stellar-core installed:
# CAPTIVE_CORE_BINARY_PATH=/usr/bin/stellar-core
# CAPTIVE_CORE_CONFIG_PATH=/etc/stellar-core-testnet.cfg

# Filter Configuration (Optional)
# -------------------------------
# Comma-separated list of contract addresses to filter
# Only events from these contracts will be ingested
# FILTER_CONTRACTS=CONTRACT_ID_1,CONTRACT_ID_2